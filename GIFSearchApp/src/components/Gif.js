import React, { Component } from 'react';

// functional component, doesn't require "export default"
const Gif = () => {
  return (
    <div>
      INput Gif Here
    </div>
  );
}

export default Gif;
